# Mike version labels for local `task docs:mike:deploy*` only.
#
# local   — `task docs:mike:deploy-local` (no git push)
# release — `task docs:mike:deploy` unless VERSION is set in the environment
#
# CI: `.github/workflows/docs-deploy.yml` sets VERSION from the git tag, release, or manual input.
# It does not read this file.

[versions]
local = dev
release =
